

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  
  
  
}

model User {
  id        String   @id @map("_id") @default(uuid())
  Firstname String
  Lastname  String
  Email     String   @unique
  Password  String
}
